# O83 Work Alert — ตั้งค่าแบบ No-Code

ระบบอ่านรายชื่อและรายการกะจาก Google Sheet ผ่าน Google Apps Script จึงแก้ข้อมูลประจำวันได้โดยไม่ต้องแก้ `index.html`

## Sheet ที่ใช้

### `พนักงาน`

คอลัมน์หลักเดิม:

| name | nick | pos | type |
| --- | --- | --- | --- |
| ชื่อจริง | ชื่อเล่น | ตำแหน่ง | `mgr`, `op` หรือ `tech` |

เพิ่ม/แก้/ลบแถวได้ตามต้องการ ระบบจะโหลดข้อมูลใหม่เมื่อเปิดหรือรีเฟรชหน้าเว็บ

### `กะ`

Apps Script จะสร้าง Sheet นี้ให้อัตโนมัติเมื่อรัน `setupSystem()` หรือเมื่อเปิด Web App ครั้งแรก

| กะ | เปิดใช้งาน | ลำดับ |
| --- | --- | ---: |
| 07:00-16:00 | TRUE | 1 |
| 08:00-17:00 | TRUE | 2 |

- แก้ข้อความในคอลัมน์ `กะ` ได้โดยตรง เช่น `09:00-18:00`
- ตั้ง `เปิดใช้งาน` เป็น `FALSE`, `ไม่ใช่`, `ปิด` หรือ `0` เพื่อซ่อนกะนั้นจากหน้าเว็บ
- คอลัมน์ `ลำดับ` ใช้เรียงปุ่มกะจากน้อยไปมาก
- ถ้าเว้นคอลัมน์ `เปิดใช้งาน` ว่าง ระบบจะถือว่าเปิดใช้งาน
- ชื่อหัวคอลัมน์รองรับทั้งภาษาไทย (`กะ`, `เปิดใช้งาน`, `ลำดับ`) และอังกฤษ (`shift`, `active`, `sort`)

## หลังแก้โค้ด Apps Script

1. นำไฟล์ `Code.gs` ใน repository ไปวางในโปรเจกต์ Apps Script เดิม
2. รัน `setupSystem()` หนึ่งครั้งและอนุญาตสิทธิ์ตามที่ Google ขอ
3. Deploy > Manage deployments > Edit > New version > Deploy
4. ตรวจสอบว่า Web App ตั้งเป็นผู้ใช้ที่มีสิทธิ์เข้าถึง Sheet และเข้าถึงได้จากหน้าเว็บ

หน้าเว็บจะเรียก `doGet?action=getStaff` แล้วรับทั้งรายชื่อและรายการกะจากคำตอบเดียวกัน

ปุ่ม `ดาวน์โหลดรูปภาพ` จะทำงานครั้งเดียวทั้งสามอย่าง:

1. บันทึกข้อมูลตารางลง Sheet `รายการเข้างาน`
2. อัปโหลดรูปภาพไป Google Drive
3. ดาวน์โหลดรูปภาพลงเครื่องผู้ใช้

ชื่อไฟล์ใน Google Drive จะเป็นรูปแบบ:

```text
YYYYMMDD_HHmmss_Smart_time_dashboard_announcement.png
```

## ลำดับการโหลด

```text
Google Sheet: พนักงาน + กะ
          ↓
Google Apps Script: doGet?action=getStaff
          ↓
GitHub Pages index.html
```

ค่า default ในหน้าเว็บยังคงอยู่เป็น fallback หาก Apps Script รุ่นเก่ายังไม่ได้ Deploy เวอร์ชันใหม่
